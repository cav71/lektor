## branch remove-inifile

Setup
mkdir -p old && cp .venv/lib/python3.14/site-packages/inifile.py old
export PYTHONPATH=$(pwd)/old
export VIRTUAL_ENV=$(pwd)/.venv


Run tests
uv run pytest -vvs -x  tests
uv run pytest -vvs -x  tests/admin/test_api.py
uv run pytest -vvs -x  tests/test_project.py


Failures:
PYTHONPATH=$(pwd) pytest -vvs tests/test_pluginsystem.py::TestPlugin::test_get_config

PYTHONPATH=$(pwd) pytest -vvs tests/test_environment.py
PYTHONPATH=$(pwd) pytest -vvs tests/test_pluginsystem.py

VIRTUAL_ENV=.venv uv run pytest -vvs -x tests/test_pluginsystem.py
1. lektor/utils.py untrusted_to_os_path

o lektor/databags.py
o lektor/pluginsystem.py

VIRTUAL_ENV=.venv uv run pytest -vvs -x tests/admin/test_api.py
